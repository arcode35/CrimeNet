# Legacy v1 experiment records

These configs are retained only to explain historical experiment artifacts.
They target the retired Delta/GCS table, are not accepted by the current
Parquet model-table access contract, and must not be used for new runs or HPO.

Use `intensity_transfer_v2.yaml` or `mark_subtype_transfer_v2.yaml` instead.
