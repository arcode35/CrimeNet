targets:
  dev:
    default: true
    mode: development

    workspace:
      host: https://adb-7405618481105118.18.azuredatabricks.net
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}

    variables:
      catalog: crimenet_dev
      raw_landing_path: /Volumes/crimenet_dev/raw_files/landing
